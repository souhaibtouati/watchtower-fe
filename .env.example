# Watchtower Frontend Environment Variables
# Copy this file to .env and adjust values as needed

# Watchtower API URL (backend service)
# Examples:
#   - http://watchtower-api:8080  (Docker network)
#   - http://localhost:8080       (Local development)
#   - http://192.168.1.100:8080   (Remote server)
API_URL=http://watchtower-api:8080

# Watchtower HTTP API Token
# This must match the WATCHTOWER_HTTP_API_TOKEN set on your Watchtower instance
# Required for authentication with the Watchtower API
API_TOKEN=your-secret-token-here

# API request timeout
API_TIMEOUT=60s
